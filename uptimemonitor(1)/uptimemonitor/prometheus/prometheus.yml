global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  - job_name: "uptime-monitor-api"
    static_configs:
      - targets: ["api:8000"]   # api = docker-compose service name
    metrics_path: "/metrics"
